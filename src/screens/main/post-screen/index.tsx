import { Fragment, useRef } from "react";
import { KeyboardAvoidingView, Platform, Text } from "react-native";

export default function PostScreenTab() {
  const rich = useRef<any>(null);
  return (
    <Fragment>
      <Text>OKE LETSGO</Text>
    </Fragment>
  );
}
